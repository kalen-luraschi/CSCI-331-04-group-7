# 2048 python file 

Structure:
imports (pygame, random, math, deepcopy)
constants (colors, fonts, window size)

class Tile `draws each tile`

class Game2048 `handles the game logic`

draw_grid() `renders everything on the screen`

main()